# Polymarket MCP

MCP server made to access prediction market data using the Polymarket API. All the tools in this MCP server are using the `gamma` and `data` read-only APIs only. No `clob` API integration at the moment.

